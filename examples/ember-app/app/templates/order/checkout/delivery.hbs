{{page-title "Delivery"}}
<main>
  <h1>Here's your cake!</h1>
  <p>What a beauty. Bet you eat it all in one go.</p>
  <p>But hold up - we just need to ask a couple more things.</p>

  <h2>When do you need it delivered?</h2>

  <muon-inputter validation="[&quot;isRequired&quot;,&quot;minDate(&apos;01/01/2022&apos;)&quot;]" {{on "inputter-change" (route-action "updateDeliveryDate")}}>
    <label slot="label">Date</label>
    <input type="date">
  </muon-inputter>

  <h2>Any allergies or intolerances?</h2>

  <muon-inputter {{on "inputter-change" (route-action "updateAdditional")}}>
    <label slot="label">Provide further information</label>
    <textarea placeholder="Please specify so we can use the right ingredients"></textarea>
  </muon-inputter>

  <muon-inputter helper="How many portions?" {{on "inputter-change" (route-action "updatePortion")}}>
    <div slot="tip-details">We can pre-slice to suit.</div>
    <label slot="label">Slices</label>
    <input type="number" min="1" max="16">
  </muon-inputter>

  <Order::Delivery::Contact @contact={{this.model.delivery.contact}} @controller={{this}} />
  <Order::Delivery::Address @address={{this.model.delivery.address}} @controller={{this}} />
  
  <p>Price: <span>&pound;40.00</span></p>

  <LinkTo @route="order.configurator"><muon-cta icon="arrow-left">Reconfigure</muon-cta></LinkTo>
  <LinkTo @route="order.confirmation"><muon-cta>Checkout</muon-cta></LinkTo>
</main>